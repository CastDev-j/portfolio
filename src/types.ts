export type Socials = {
  name: string;
  icon: string;
  text: string;
  href: string;
}[];

export type Certificates = {
  title: string;
  url: string;
  logo: string;
}[];

export type Locale = "es" | "en";
